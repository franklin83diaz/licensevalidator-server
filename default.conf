# HTTPS Configuration
[https]
cert_path = "cert.pem"
key_path = "key.pem"

# JWT Configuration
[jwt]
private_key_path = "jwt_private.pem"
public_key_path = "jwt_public.pem"
algorithm = "RS256"  # Currently only supports RS256